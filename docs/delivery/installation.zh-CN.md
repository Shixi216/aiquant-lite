# 安装说明

运行 `HermesOPC-0.10.0-Setup.exe`，选择安装目录和快捷方式。

安装包为当前未提交工作树生成的 candidate 构建，不是稳定发布标签。最终用户无需预装 Python、uv、Git、Node.js 或 Visual Studio。

程序文件默认安装到 `%LOCALAPPDATA%\Programs\HermesOPC`，用户数据位于 `%LOCALAPPDATA%\HermesOPC`。
